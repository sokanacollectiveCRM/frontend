# Contract Payment (Deprecated)

> **This document is superseded.** Sokana does not use Stripe or a client-facing `/contract-payment` checkout page.

Use the staff-facing SOP instead:

**[`docs/FAMILY_ONBOARDING_SOP.md`](../../../docs/FAMILY_ONBOARDING_SOP.md)**

That SOP covers:

- Public intake through portal invite
- SignNow contract flow
- Payment authorization forms and staff-coordinated billing
- QuickBooks customer sync (on **Matched**)
- Portal eligibility (contract signed + first payment succeeded)
- Legacy Stripe code to ignore (`createPaymentIntent`, etc.)
