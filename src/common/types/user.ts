export type User = {
  id: string,
  firstname: string,
  lastname: string,
  email: string,
  profile_picture?: string,
  bio?: string,
  role: string,
  state?: string,
  address?: string,
  city?: string,
}